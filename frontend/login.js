// Simple Login UI Logic (Demo)
function login(email, password) {
    if (!email || !password) {
        console.log("Validation failed");
        return;
    }
    console.log("Calling login API...");
}